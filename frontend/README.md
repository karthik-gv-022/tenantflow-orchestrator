# TenantFlow Orchestrator Frontend

React + Vite + TypeScript frontend for TenantFlow Orchestrator.

## Run Locally

```sh
npm install
npm run dev
```

## Environment

The frontend targets the FastAPI backend at:

`http://localhost:8000`

Set this explicitly with:

`VITE_API_BASE_URL=http://localhost:8000`
